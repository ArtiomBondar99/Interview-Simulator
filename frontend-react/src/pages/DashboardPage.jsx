import EmptyState from "../components/EmptyState.jsx";

// Placeholder -- will call interviewService.listInterviews() and render aggregate stats
// (total interviews, average/best score, recent improvement).
export default function DashboardPage() {
  return <EmptyState title="Dashboard" description="This page is being built next." />;
}
