import EmptyState from "../components/EmptyState.jsx";

// Placeholder -- will call interviewService.listInterviews() and render the interview list,
// linking each row to /results/:id.
export default function InterviewHistoryPage() {
  return <EmptyState title="Interview history" description="This page is being built next." />;
}
