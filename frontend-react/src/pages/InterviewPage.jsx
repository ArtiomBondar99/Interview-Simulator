import EmptyState from "../components/EmptyState.jsx";

// Placeholder for this sub-phase of the React migration -- the live question/answer/timer flow,
// including practice vs. real interview mode, is the most complex remaining piece and gets its
// own dedicated increment.
export default function InterviewPage() {
  return <EmptyState title="Interview in progress" description="This page is being built next." />;
}
