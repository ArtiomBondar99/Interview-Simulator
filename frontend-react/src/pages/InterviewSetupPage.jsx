import EmptyState from "../components/EmptyState.jsx";

// Placeholder for this sub-phase of the React migration -- the setup form (role, level,
// question count, candidate profile) is built in the next increment. See interviewService.js's
// startInterview()/generateQuestions() for the calls it will make.
export default function InterviewSetupPage() {
  return <EmptyState title="Interview setup" description="This page is being built next." />;
}
